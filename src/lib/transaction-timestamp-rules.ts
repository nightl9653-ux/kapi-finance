/**
 * 交易 `timestamp`（timestamptz）与 `occurred_on`（date）的写入约定。
 * 与 `supabase/schema.sql` 中 `transactions` 表注释一致。
 *
 * **跨境卡 / 多源时间（UTC、境外墙钟、无时区字符串）**
 * - 先统一得到 **唯一绝对瞬间** 再写入 `timestamp`：若 API 给 RFC3339（含 `Z` 或 offset）可直接解析；若给「某地墙钟 + 该地时区」，按该 IANA 时区解析；**禁止**把无时区字符串当成服务器本地时间猜。
 * - `occurred_on`：**不是** `timestamp` 在 UTC 下的自然日切片，而是把该绝对瞬间 **投影到「记账业务时区」**（用户/账户配置的 IANA 时区）后取 **当地日历日** `YYYY-MM-DD`。同一笔在 UTC 与业务时区可能跨自然日，**以业务时区下的日期为准**。
 * - 导入层应保留渠道元数据（源时区、原始串）便于排错；落库为规范后的 instant + 业务日。
 *
 * **仅有日期（无时刻）的数据源（如部分账单）**
 * - 仍存完整瞬间：在 **已明确的日历日 + 时区语义**（通常为记账业务时区或账单展示时区）下取中性墙钟 **12:00:00** → 对应 UTC 瞬间写入 `timestamp`。
 * - `occurred_on` = 与上述墙钟在 **业务时区** 下的日期一致，避免「日期来自账单、时刻却是导入服务器时间」。
 *
 * **有完整消费时间的数据源**
 * - `timestamp` = 解析得到的真实消费瞬间（UTC 语义入库）。
 * - `occurred_on` = 该瞬间在 **记账业务时区** 下的日历日（已约定：以消费时间为准，不以银行入账时间为准）。
 *
 * **手工「记一笔」**
 * - `datetime-local` 通常精确到分钟，秒可为 0；仍为合法 `timestamptz`；`occurred_on` 与表单侧本地日历日对齐。
 *
 * **OCR / 扫单（仅开发约定，勿直接当用户可见文案）**
 * - 无 API 元数据时：原始墙钟按团队统一的一条解析规则入库即可；**以用户编辑为最终准**。
 * - 对客不必默认占用注意力；仅在识别明显异常、与当前筛选月份等对不上、或用户进入详情时再提示/修改。
 */

/** 中性占位：业务日在用户/账户时区内的 12:00:00（墙钟）。自动导入仅有日期时使用。 */
export const NEUTRAL_TRANSACTION_LOCAL_TIME = {
  hour: 12,
  minute: 0,
  second: 0,
} as const;
